{
  "name": "thrive-realtime-voice-platform",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "packages/*",
    "apps/demo-voice"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
